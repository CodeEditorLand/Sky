import{isUtilityProcess}from"../../sandbox/node/electronTypes.js";import{VSBuffer}from"../../../common/buffer.js";import{IPCServer}from"../common/ipc.js";import{Emitter,Event}from"../../../common/event.js";import{assertType}from"../../../common/types.js";class Protocol{constructor(e){this.port=e,this.onMessage=Event.fromNodeEventEmitter(this.port,"message",(e=>e.data?VSBuffer.wrap(e.data):VSBuffer.alloc(0))),e.start()}send(e){this.port.postMessage(e.buffer)}disconnect(){this.port.close()}}export class Server extends IPCServer{static getOnDidClientConnect(e){assertType(isUtilityProcess(process),"Electron Utility Process");const t=new Emitter;return process.parentPort.on("message",(o=>{if(e?.handledClientConnection(o))return;const r=o.ports.at(0);r&&t.fire(r)})),Event.map(t.event,(e=>({protocol:new Protocol(e),onDidClientDisconnect:Event.fromNodeEventEmitter(e,"close")})))}constructor(e){super(Server.getOnDidClientConnect(e))}}export function once(e,t,o){const r=s=>{s.data===t&&(e.removeListener("message",r),o())};e.on("message",r)}