import{TestInstantiationService}from"../../../../../platform/instantiation/test/common/instantiationServiceMock.js";import{DomActivityTracker}from"../../browser/domActivityTracker.js";import{UserActivityService}from"../../common/userActivityService.js";import*as sinon from"sinon";import assert from"assert";suite("DomActivityTracker",(()=>{let e,t,i,s;const r=9e4;setup((()=>{s=sinon.useFakeTimers(),i=new TestInstantiationService,e=new UserActivityService(i),t=new DomActivityTracker(e)})),teardown((()=>{t.dispose(),e.dispose(),s.restore(),i.dispose()})),test("marks inactive on no input",(()=>{assert.equal(e.isActive,!0),s.tick(r),assert.equal(e.isActive,!1)})),test("preserves activity state when active",(()=>{assert.equal(e.isActive,!0);for(let e=0;e<10;e++)document.dispatchEvent(new MouseEvent("keydown")),s.tick(9e3);assert.equal(e.isActive,!0)})),test("restores active state",(()=>{assert.equal(e.isActive,!0),s.tick(r),assert.equal(e.isActive,!1),document.dispatchEvent(new MouseEvent("keydown")),assert.equal(e.isActive,!0),s.tick(r),assert.equal(e.isActive,!1)}))}));